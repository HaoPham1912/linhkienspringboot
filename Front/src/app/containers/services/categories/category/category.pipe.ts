import { Pipe, PipeTransform } from '@angular/core';
import { Category } from '../../../../views/homepage/category';

@Pipe({
  name:'Test'
})
export class CategoryPipe   {

  // transform(category: Category[], text: string): Category[]{
      
  // }

}
